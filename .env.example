# .env.example
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
GOOGLE_DRIVE_API_KEY=
# service role key do Supabase (Project Settings → API) — bypassa RLS, nunca expor
# ao client. Necessária só pro servidor MCP (/api/mcp) autenticar por token próprio.
SUPABASE_SERVICE_ROLE_KEY=
